# frozen_string_literal: true

class AddAutoGeneratedToSystemNodeModules < ActiveRecord::Migration[8.0]
  def change
    # Drives default-hidden behavior in the catalog UI (auto_generated modules
    # don't appear in the standard Modules tab) and gates the alternate
    # PackageBuildWebhookService ingest path keyed by module_id rather than
    # gitea_repo_full_name.
    add_column :system_node_modules, :auto_generated, :boolean,
      null: false, default: false
    add_index  :system_node_modules, :auto_generated
  end
end
